npm install express express-async-errors awilix cors compression helmet bcryptjs jsonwebtoken swagger-ui-express
npm install mysql

//The simplified HTTP request client 'request' with Promise support. Powered by Bluebird.
npm i request-promise


npm i -D dotenv nodemon 


swagger
npm i swagger-ui-express -S

creare swagger.json

var swaggerUi = require('swagger-ui-express'),
    swaggerDocument = require('./swagger.json');

app.use('/api-docs', swaggerUi.serve, swaggerUi.setup(swaggerDocument));
app.use('/api/v1', router);

http://localhost:5000/api/api-docs/

jsdoc
instalamos jsdoc 
npm install -g jsdoc
creamos una carpeta con un jsdoc.json y en el ponemos la configuracion paracrear nuestra documentacion


ahora bien para que se arranque con un solo comando de forma rapida en nuestro package.json le ponemos un comando que active la ruta
"docs": "jsdoc -c src/jsDoc/jsdoc.json"